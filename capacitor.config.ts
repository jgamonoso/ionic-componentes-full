import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-componentes-full',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
